/**
 * Emergency intent — direct user to call emergency services.
 * No APIs, free response.
 */

export function handleEmergency(): string {
  return 'This may be serious. Please call emergency services now.';
}
