/**
 * Triage intent — symptom assessment prompt.
 * No APIs, free response.
 */

export function handleTriage(): string {
  return 'I will assess your symptoms. Tell me since when you have been feeling this way.';
}
