# Expo app: point to your backend. Copy to .env and set.
#   Windows: copy .env.example .env
#   macOS/Linux: cp .env.example .env
#
# Correct URLs:
#   Android Emulator:  http://10.0.2.2:8000  (emulator's host = your machine)
#   iOS Simulator:     http://localhost:8000
#   Real phone:       http://<LAN-IP>:8000  (e.g. http://192.168.1.5:8000)
#                     Find LAN IP: run ipconfig (Windows) and use IPv4 Address
#
# Windows firewall (if phone/emulator can't reach backend):
#   Run as Administrator:
#   netsh advfirewall firewall add rule name="REX Backend" dir=in action=allow protocol=TCP localport=8000
#
EXPO_PUBLIC_API_URL=http://localhost:8000
