# Copy to .env and fill in. Keys must only exist here (never in Expo app).

# Required for chat, SOAP, vision, extract, voice/realtime
OPENAI_API_KEY=

# Fallback when OpenAI fails (chat, SOAP, vision)
GEMINI_API_KEY=

# LiveKit (for /api/voice/token). Optional for MVP.
LIVEKIT_API_KEY=
LIVEKIT_API_SECRET=
LIVEKIT_URL=

PORT=8000
